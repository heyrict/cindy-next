import React from 'react';
import PropTypes from 'prop-types';
import { Flex, Box, Button } from 'components/General';

const KeywordSelectToolbar = () => {
  return (
    <Flex width={1}>
      <Box>Keyword appearance count</Box>
    </Flex>
  );
};

export default KeywordSelectToolbar;
